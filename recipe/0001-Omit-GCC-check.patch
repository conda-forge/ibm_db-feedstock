--- a/setup.py
+++ b/setup.py
@@ -338,6 +338,16 @@
             else:
                 cliFileName = 's390_odbc_cli.tar.gz'
                 arch_ = 's390'
+        elif ('aarch64' in os.uname()[4]):
+            os_ = 'linux'
+            if is64Bit:
+                cliFileName = 'linuxarm64_odbc_cli.tar.gz'
+                arch_ = 'arm64'
+                if(clidriver_version.startswith("v11")):
+                    clidriver_version = "v12.1.0"
+            else:
+                cliFileName = 'linuxarm32_odbc_cli.tar.gz'
+                arch_ = 'armv7l'
     elif ('sunos' in sys.platform):
         os_ = 'solaris*'
         if ('i86pc' in os.uname()[4]):
@@ -520,12 +528,6 @@
 else:
     ext_modules = _ext_modules(ibm_db_include, library, ibm_db_lib, ibm_db_lib_runtime)
         
-if('win32' not in sys.platform):
-    if sys.version_info >= (3, ):        
-        if (sys.platform != 'zos'):
-           _checkGcc()
-           _checkPythonHeaderFile()
-
 # Custom build_py to include ibm_db_dll.pth at the wheel root
 # so it lands in site-packages and triggers DLL registration on startup.
 class _build_py_with_pth(build_py):
